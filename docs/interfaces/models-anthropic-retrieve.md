# Anthropic Model Retrieve

- Official docs: https://platform.minimaxi.com/docs/api-reference/models/anthropic/retrieve-model.md
- Endpoint: `GET /anthropic/v1/models/{model_id}`
- SDK status: `Not implemented`
- Local code: none.

## Purpose

Retrieve a single model through the Anthropic-compatible endpoint.

## Development notes

Validate `model_id` locally and preserve raw metadata for compatibility drift.

